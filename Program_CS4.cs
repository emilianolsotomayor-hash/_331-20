using System;

namespace CS4 
{
    class Program
    {
        static void Main(string[] args)
        {
            // Sesión 7 : Estructras selectivas
            // a. Simple: Instrucción if
            int a = 0;
            int b = 1;
            
            if (a == 0) // si el valor de a es igual a 1 entonces ...
            {
                // Bloque de instrucciones.
                Console.WriteLine("El valor de a es iguañ a 0");
                a += 1;
                Console.WriteLine($"a: {a}");

            }
            // b. Doble : Instrucciones if-else
            // c. Multiple: Instrucciones if-else if-else 
        }
    }
}
